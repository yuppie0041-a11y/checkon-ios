name: Build and Upload to App Store

on:
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: macos-15

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install CocoaPods
        run: |
          sudo gem install cocoapods --no-document > /dev/null 2>&1
          pod install > /dev/null 2>&1
          echo "pod install done"

      - name: Fix project - remove Pods framework and set signing
        run: |
          cat > /tmp/fix_project.rb << 'RUBY'
          require 'xcodeproj'
          proj_path = "마음안전365 체크온.xcodeproj"
          project = Xcodeproj::Project.open(proj_path)
          target = project.targets.find { |t| t.name == "마음안전365 체크온" }
          abort "ERROR: target not found" unless target
          phase = target.frameworks_build_phase
          before = phase.files.map { |f| f.display_name.to_s }
          puts "Frameworks before (#{before.count}): #{before.inspect}"
          to_remove = phase.files.select { |f| f.display_name.to_s =~ /Pods_/ }
          puts "Removing #{to_remove.count} aggregate framework(s)"
          to_remove.each do |bf|
            name = bf.display_name.to_s
            ref = bf.file_ref
            bf.remove_from_project
            begin; ref.remove_from_project; rescue; end
            puts "  Removed: #{name}"
          end
          after = target.frameworks_build_phase.files.map { |f| f.display_name.to_s }
          puts "Frameworks after (#{after.count}): #{after.inspect}"
          target.build_configurations.each do |config|
            next unless config.name == "Release"
            config.build_settings['CODE_SIGN_STYLE'] = 'Manual'
            config.build_settings['CODE_SIGN_IDENTITY'] = 'Apple Distribution'
            config.build_settings['PROVISIONING_PROFILE_SPECIFIER'] = 'MindSafe365 Checkon AppStore'
            config.build_settings['DEVELOPMENT_TEAM'] = 'UX7GD558S8'
            config.build_settings['PRODUCT_NAME'] = 'Checkon'
            puts "Signing configured for Release (PRODUCT_NAME=Checkon)"
          end
          project.save
          puts "Project saved OK"
          RUBY
          ruby /tmp/fix_project.rb

      - name: Import Distribution Certificate
        env:
          P12_BASE64: ${{ secrets.APPLE_CERTIFICATES_P12 }}
          P12_PASSWORD: ${{ secrets.APPLE_CERTIFICATES_P12_PASSWORD }}
        run: |
          echo "$P12_BASE64" | base64 --decode > certificate.p12
          security create-keychain -p "" build.keychain
          security default-keychain -s build.keychain
          security unlock-keychain -p "" build.keychain
          security set-keychain-settings -lut 21600 build.keychain
          security import certificate.p12 -k build.keychain -P "$P12_PASSWORD" -T /usr/bin/codesign
          security set-key-partition-list -S apple-tool:,apple: -s -k "" build.keychain

      - name: Show installed certificates
        run: |
          echo "=== Codesigning identities ==="
          security find-identity -v -p codesigning build.keychain || true
          echo "=== All identities ==="
          security find-identity -v build.keychain || true

      - name: Install Provisioning Profile
        env:
          PP_BASE64: ${{ secrets.APPLE_PROVISIONING_PROFILE }}
        run: |
          mkdir -p ~/Library/MobileDevice/Provisioning\ Profiles
          echo "$PP_BASE64" | base64 --decode > ~/Library/MobileDevice/Provisioning\ Profiles/checkon.mobileprovision
          echo "=== Provisioning Profile Info ==="
          PP_FILE="$HOME/Library/MobileDevice/Provisioning Profiles/checkon.mobileprovision"
          /usr/bin/security cms -D -i "$PP_FILE" 2>/dev/null \
            | /usr/bin/plutil -p - 2>/dev/null \
            | grep -E '"(Name|TeamName|ProvisionsAllDevices|ProvisionedDevices|get-task-allow|ExpirationDate)"' \
            | head -20 \
            || echo "Could not parse profile"

      - name: Build Archive
        run: |
          set -o pipefail
          xcodebuild archive \
            -workspace "마음안전365 체크온.xcworkspace" \
            -scheme "마음안전365 체크온" \
            -configuration Release \
            -destination "generic/platform=iOS" \
            -sdk iphoneos \
            -archivePath "$RUNNER_TEMP/checkon.xcarchive" \
            2>&1 | tee "$RUNNER_TEMP/build.log"

      - name: Check archive signing
        run: |
          APP=$(find "$RUNNER_TEMP/checkon.xcarchive/Products/Applications" -name "*.app" -maxdepth 1 | head -1)
          echo "App: $APP"
          echo "=== codesign -dvvv (top-level) ==="
          codesign -dvvv "$APP" 2>&1 | grep -E "(Authority|TeamIdentifier|Identifier|Signed Time|Timestamp)" | head -20 || true

          echo ""
          echo "=== DEEP verify (nested 포함 전체 검증) ==="
          codesign --verify --deep --strict --verbose=4 "$APP" 2>&1 | head -40 || echo "DEEP VERIFY FAILED (위 에러 확인)"

          echo ""
          echo "=== 번들 내 서명 대상 구성요소 목록 ==="
          echo "--- Frameworks 폴더 ---"
          ls -la "$APP/Frameworks" 2>/dev/null || echo "  (Frameworks 폴더 없음 - static linkage 정상)"
          echo "--- PlugIns 폴더 ---"
          ls -la "$APP/PlugIns" 2>/dev/null || echo "  (PlugIns 없음)"
          echo "--- .bundle 리소스 ---"
          find "$APP" -name "*.bundle" -maxdepth 2 2>/dev/null || echo "  (없음)"
          echo "--- dylib/framework 파일 ---"
          find "$APP" \( -name "*.dylib" -o -name "*.framework" \) 2>/dev/null | head -20 || echo "  (없음)"

          echo ""
          echo "=== 각 nested 구성요소 서명 확인 ==="
          find "$APP" \( -name "*.framework" -o -name "*.dylib" -o -name "*.appex" -o -name "*.bundle" \) 2>/dev/null | while read comp; do
            echo "[$comp]"
            codesign -dvv "$comp" 2>&1 | grep -E "(Authority|not signed|code object)" | head -3 || echo "  서명 확인 불가"
          done

          echo ""
          echo "=== embedded.mobileprovision ==="
          PP="$APP/embedded.mobileprovision"
          if [ -f "$PP" ]; then
            /usr/bin/security cms -D -i "$PP" 2>/dev/null \
              | /usr/bin/plutil -p - 2>/dev/null \
              | grep -E '"(Name|TeamName|get-task-allow)"' \
              | head -10 || echo "Could not parse embedded profile"
          else
            echo "embedded.mobileprovision NOT FOUND"
          fi

      - name: Sign nested bundles in archive (before exportArchive)
        run: |
          security unlock-keychain -p "" build.keychain
          security default-keychain -s build.keychain
          IDENTITY=$(security find-identity -v -p codesigning build.keychain | grep "Apple Distribution" | head -1 | awk '{print $2}')
          echo "Signing identity: $IDENTITY"
          APP=$(find "$RUNNER_TEMP/checkon.xcarchive/Products/Applications" -name "*.app" -maxdepth 1 | head -1)
          echo "App: $APP"

          echo ""
          echo "=== 앱 번들에서 불필요한 Entitlements 폴더 제거 ==="
          if [ -d "$APP/Entitlements" ]; then
            rm -rf "$APP/Entitlements"
            echo "제거 완료 (서명 설정 파일 - 앱에 포함되면 안 됨)"
          else
            echo "  (없음)"
          fi

          echo ""
          echo "=== 중첩 번들 서명 (메인 앱은 exportArchive가 재서명) ==="
          find "$APP" -name "*.bundle" | while read b; do
            NAME=$(basename "$b")
            if codesign --force --sign "$IDENTITY" --timestamp=none "$b" 2>/tmp/err; then
              codesign --verify --strict "$b" 2>/dev/null && echo "  OK: $NAME" || echo "  WARN(검증실패): $NAME"
            else
              echo "  FAIL: $NAME - $(cat /tmp/err)"
            fi
          done

      - name: Show build errors (on failure)
        if: failure()
        run: |
          echo "=== Build errors ==="
          grep -E "(error:|not found|ld: |clang: |Undefined symbol)" "$RUNNER_TEMP/build.log" | head -80 || true
          echo ""
          echo "=== Last 100 lines ==="
          tail -100 "$RUNNER_TEMP/build.log" || true

      - name: Export and Upload to App Store Connect
        env:
          ASC_KEY_ID: ${{ secrets.ASC_KEY_ID }}
          ASC_ISSUER_ID: ${{ secrets.ASC_ISSUER_ID }}
          ASC_KEY_BASE64: ${{ secrets.ASC_KEY_BASE64 }}
        run: |
          mkdir -p ~/private_keys
          echo "$ASC_KEY_BASE64" | base64 --decode > ~/private_keys/AuthKey_${ASC_KEY_ID}.p8
          cat > ExportOptions.plist << 'PLIST'
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0">
          <dict>
            <key>method</key>
            <string>app-store-connect</string>
            <key>destination</key>
            <string>upload</string>
            <key>teamID</key>
            <string>${{ secrets.APPLE_TEAM_ID }}</string>
            <key>signingStyle</key>
            <string>manual</string>
            <key>signingCertificate</key>
            <string>Apple Distribution</string>
            <key>provisioningProfiles</key>
            <dict>
              <key>kr.co.mindsafe365.checkon</key>
              <string>MindSafe365 Checkon AppStore</string>
            </dict>
          </dict>
          </plist>
          PLIST
          xcodebuild -exportArchive \
            -archivePath "$RUNNER_TEMP/checkon.xcarchive" \
            -exportOptionsPlist ExportOptions.plist \
            -authenticationKeyPath "$HOME/private_keys/AuthKey_${ASC_KEY_ID}.p8" \
            -authenticationKeyID "$ASC_KEY_ID" \
            -authenticationKeyIssuerID "$ASC_ISSUER_ID"
